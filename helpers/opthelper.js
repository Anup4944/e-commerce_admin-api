const randNum =  () => {
    const num = Math.floor(Math.random() * 10);
}





const getRandOTP = (length) => {
    const otp =  "" ; 
    /// return six digit 
    for (let i= 0; i < array.length; i++) {
        otp += 
        
    }
    
}